name: Download Youtube
description: Downloads a youtube video.
inputs:
  url:
    type: string
    required: true
    description: URL of the video
  path:
    type: string
    required: false
    description: Path to download to
outputs:
  completed:
    type: boolean
    description: Returns true if video is downloaded.
runs:
  using: python
  main: main.py
